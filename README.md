# Kvartett Website

Современный сайт рекламного агентства на **Next.js 15** (App Router) + **Drizzle ORM** + **PostgreSQL**.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных

Выберите один из вариантов подключения к PostgreSQL:

#### Вариант A: Облачная БД (Рекомендуется)
- **Neon** - [neon.tech](https://neon.tech) (бесплатно 0.5GB)
- **Supabase** - [supabase.com](https://supabase.com) (бесплатно 500MB)
- **Vercel Postgres** - интеграция с Vercel

#### Вариант B: Локальный PostgreSQL
Установите PostgreSQL локально. Подробности в [DATABASE_SETUP.md](./DATABASE_SETUP.md)

### 3. Настройка переменных окружения

Скопируйте `example.env` в `.env` и заполните:

```bash
cp example.env .env
```

Основные переменные:
```env
DATABASE_URL=postgresql://username:password@host:5432/kvartett
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key
```

### 4. Инициализация базы данных

```bash
# Push схему в БД
npm run db:push

# Заполнить тестовыми данными
npm run db:seed
```

### 5. Запуск dev сервера

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 📦 Доступные команды

```bash
npm run dev        # Запуск dev сервера (Turbopack)
npm run build      # Сборка для продакшена
npm run start      # Запуск production сервера
npm run lint       # Проверка кода

# База данных (Drizzle)
npm run db:push    # Применить изменения схемы
npm run db:studio  # Открыть Drizzle Studio (GUI для БД)
npm run db:seed    # Заполнить БД тестовыми данными
npm run db:generate # Сгенерировать миграции
```

## 🛠 Технологии

- **Next.js 15** - React framework с App Router
- **Drizzle ORM** - Type-safe ORM для PostgreSQL
- **NextAuth.js** - Аутентификация
- **Tailwind CSS 4** - Стилизация
- **TypeScript** - Типизация
- **Framer Motion** - Анимации
- **TipTap** - Rich text editor

## 📁 Структура проекта

```
kvartett-website/
├── src/
│   ├── app/              # App Router страницы
│   │   ├── api/          # API routes
│   │   ├── admin/        # Админ панель
│   │   └── components/   # React компоненты
│   ├── db/               # База данных
│   │   ├── schema.ts     # Drizzle схема
│   │   ├── index.ts      # DB клиент
│   │   └── seed.ts       # Seed данные
│   └── lib/              # Утилиты
├── public/               # Статические файлы
└── drizzle.config.ts     # Конфигурация Drizzle
```

## 🌐 Деплой

### Vercel (Рекомендуется)

1. Push код на GitHub
2. Импортируйте проект в [Vercel](https://vercel.com)
3. Добавьте переменные окружения
4. Деплой!

### Другие платформы

- **Railway** - автоматический деплой + PostgreSQL
- **Render** - бесплатный tier
- **Fly.io** - serverless деплой

Подробнее в [DATABASE_SETUP.md](./DATABASE_SETUP.md)

## 👤 Админ панель

После seed'а используйте:
- **Email:** admin@kvartett.ru
- **Пароль:** admin123

⚠️ **Важно:** Измените пароль в продакшене!

## 📝 Лицензия

Private project.
