# Цветовая палитра сайта Квартет

Централизованная система управления цветами для всего сайта.

## 📁 Файлы

- `colors.css` - Основной файл с цветовой палитрой

## 🎨 Основные цвета

Все цвета взяты из Hero секции сайта:

- **Основной синий**: `#3b82f6` (`--color-primary`)
- **Темно-синий**: `#1e40af` (`--color-primary-dark`) 
- **Светло-синий**: `#93c5fd` (`--color-primary-light`)
- **Фоновый**: `#eff6ff` (`--color-primary-bg`)

## 🚀 Использование

### CSS переменные
```css
/* В CSS файлах */
.my-element {
  background-color: var(--color-primary);
  color: var(--color-text-primary);
  border: 1px solid var(--color-border-primary);
}
```

### Утилитарные классы
```jsx
// В React компонентах
<div className="bg-primary text-white shadow-primary">
  Контент
</div>
```

### Inline стили
```jsx
// В React компонентах
<div style={{
  backgroundColor: 'var(--color-primary)',
  color: 'var(--color-text-white)'
}}>
  Контент
</div>
```

## 🔄 Миграция существующего кода

### Замена hardcoded цветов

**Было:**
```jsx
className="bg-[#3b82f6] text-[#1e40af]"
```

**Стало:**
```jsx
className="bg-primary text-primary-dark"
// или
style={{
  backgroundColor: 'var(--color-primary)',
  color: 'var(--color-primary-dark)'
}}
```

### Замена градиентов

**Было:**
```jsx
className="bg-gradient-to-r from-[#3b82f6] to-[#1e40af]"
```

**Стало:**
```jsx
className="bg-gradient-primary"
// или
style={{background: 'var(--gradient-primary)'}}
```

## 📋 Доступные переменные

### Основные цвета
- `--color-primary` - Основной синий
- `--color-primary-dark` - Темно-синий
- `--color-primary-light` - Светло-синий
- `--color-primary-bg` - Фоновый светло-синий

### Градиенты
- `--gradient-primary` - Основной градиент
- `--gradient-primary-reverse` - Обратный градиент
- `--gradient-light` - Светлый градиент
- `--gradient-bg` - Фоновый градиент

### Текстовые цвета
- `--color-text-primary` - Основной текст
- `--color-text-secondary` - Вторичный текст
- `--color-text-muted` - Приглушенный текст
- `--color-text-white` - Белый текст

### Фоновые цвета
- `--color-bg-primary` - Основной фон
- `--color-bg-secondary` - Вторичный фон
- `--color-bg-card` - Фон карточек
- `--color-bg-card-hover` - Фон карточек при наведении

### Границы
- `--color-border-light` - Светлые границы
- `--color-border-primary` - Основные границы
- `--color-border-hover` - Границы при наведении

### Тени
- `--shadow-primary` - Основная тень
- `--shadow-primary-lg` - Большая тень
- `--shadow-primary-xl` - Очень большая тень
- `--shadow-card` - Тень карточки
- `--shadow-card-hover` - Тень карточки при наведении

## 🎯 Преимущества

1. **Централизованное управление** - Все цвета в одном месте
2. **Легкое изменение темы** - Измените переменные и весь сайт перекрасится
3. **Консистентность** - Одинаковые цвета по всему сайту
4. **Производительность** - CSS переменные быстрее hardcoded значений
5. **Удобство разработки** - Понятные имена вместо hex кодов

## 🔧 Изменение цветовой схемы

Для изменения цветов всего сайта:

1. Откройте `src/styles/colors.css`
2. Измените значения в разделе "ОСНОВНАЯ ЦВЕТОВАЯ ПАЛИТРА"
3. Сохраните файл
4. Весь сайт автоматически обновится с новыми цветами

## ⚠️ Важные замечания

- Не используйте hardcoded цвета в новом коде
- При рефакторинге заменяйте существующие hardcoded цвета на переменные
- Все новые цвета добавляйте в `colors.css`
- Следуйте именованию переменных для консистентности

## 🔍 Поиск и замена

Для быстрой миграции используйте поиск и замену в IDE:

- `bg-\[#3b82f6\]` → `bg-primary`
- `text-\[#1e40af\]` → `text-primary-dark`
- `bg-\[#93c5fd\]` → `bg-primary-light`
- `bg-\[#eff6ff\]` → `bg-primary-bg`

Или для CSS:
- `#3b82f6` → `var(--color-primary)`
- `#1e40af` → `var(--color-primary-dark)`
- `#93c5fd` → `var(--color-primary-light)`
- `#eff6ff` → `var(--color-primary-bg)`